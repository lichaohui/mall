// initial state
const state = {
  user: JSON.parse(window.localStorage.user)
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}